<Window x:Class="Sellsys.WpfClient.Views.Dialogs.ContactRecordsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="联系记录"
        Height="600"
        Width="1000"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#409EFF" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="联系记录" 
                               Foreground="White" 
                               FontSize="18" 
                               FontWeight="Bold"/>
                    <TextBlock Text="{Binding CustomerName, StringFormat='客户单位：{0}'}" 
                               Foreground="White" 
                               FontSize="14" 
                               Margin="0,5,0,0"/>
                </StackPanel>
                
                <Button Grid.Column="1"
                        Content="添加记录"
                        Command="{Binding AddContactRecordCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Background="White"
                        Foreground="#409EFF"
                        Width="80"
                        Height="32"/>
            </Grid>
        </Border>

        <!-- Contact Records List -->
        <Border Grid.Row="1" 
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="1" 
                Background="White"
                Margin="0,10,0,10">
            <Grid>
                <!-- Loading Indicator -->
                <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid.Background>
                        <SolidColorBrush Color="White" Opacity="0.7"/>
                    </Grid.Background>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="200" Height="20" Margin="0,0,0,10"/>
                        <TextBlock Text="正在加载..." 
                                   Style="{StaticResource GlobalTextStyle}" 
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <!-- Data Grid -->
                <DataGrid ItemsSource="{Binding ContactRecords}"
                          SelectedItem="{Binding SelectedContactRecord}"
                          Style="{StaticResource StandardDataGridStyle}"
                          ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                          RowStyle="{StaticResource DataGridRowStyle}">
                    <DataGrid.Columns>
                        <!-- 序号 -->
                        <DataGridTextColumn Header="序号" Width="60" IsReadOnly="True">
                            <DataGridTextColumn.Binding>
                                <Binding RelativeSource="{RelativeSource AncestorType=DataGridRow}"
                                         Converter="{StaticResource RowIndexConverter}"/>
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>

                        <!-- 联系人 -->
                        <DataGridTextColumn Header="联系人" 
                                            Binding="{Binding ContactName}" 
                                            Width="100" 
                                            IsReadOnly="True"/>

                        <!-- 联系电话 -->
                        <DataGridTextColumn Header="联系电话" 
                                            Binding="{Binding ContactPhone}" 
                                            Width="120" 
                                            IsReadOnly="True"/>

                        <!-- 联系记录 -->
                        <DataGridTextColumn Header="联系记录" 
                                            Binding="{Binding Summary}" 
                                            Width="200" 
                                            IsReadOnly="True"/>

                        <!-- 客户意向 -->
                        <DataGridTextColumn Header="客户意向" 
                                            Binding="{Binding CustomerIntention}" 
                                            Width="80" 
                                            IsReadOnly="True"/>

                        <!-- 联系时间 -->
                        <DataGridTextColumn Header="联系时间" 
                                            Binding="{Binding FormattedCreatedAt}" 
                                            Width="120" 
                                            IsReadOnly="True"/>

                        <!-- 下次预约 -->
                        <DataGridTextColumn Header="下次预约" 
                                            Binding="{Binding FormattedNextFollowUpDate}" 
                                            Width="100" 
                                            IsReadOnly="True"/>

                        <!-- 销售 -->
                        <DataGridTextColumn Header="销售" 
                                            Binding="{Binding SalesPersonName}" 
                                            Width="80" 
                                            IsReadOnly="True"/>

                        <!-- 操作 -->
                        <DataGridTemplateColumn Header="操作" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="编辑"
                                                Command="{Binding DataContext.EditContactRecordCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource BlueButtonStyle}"
                                                Width="50"
                                                Height="25"
                                                Margin="0,0,5,0"/>
                                        <Button Content="删除"
                                                Command="{Binding DataContext.DeleteContactRecordCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource DeleteButtonStyle}"
                                                Width="50"
                                                Height="25"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="关闭"
                        Command="{Binding CloseCommand}"
                        Style="{StaticResource GrayButtonStyle}"
                        Width="80"
                        Height="32"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
